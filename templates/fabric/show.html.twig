{% extends 'base.html.twig' %}

{% block title %}Fabric{% endblock %}

{% block body %}
    <h1>Fabric</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fabric.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fabric.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fabric_index') }}">back to list</a>

    <a href="{{ path('app_fabric_edit', {'id': fabric.id}) }}">edit</a>

    {{ include('fabric/_delete_form.html.twig') }}
{% endblock %}
