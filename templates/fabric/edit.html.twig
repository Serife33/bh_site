{% extends 'base.html.twig' %}

{% block title %}Modifier {{ fabric.name }}{% endblock %}

{% block body %}
    <h1>Modifier {{ fabric }}</h1>

    {{ include('fabric/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('app_fabric_index') }}">Retour à la liste</a>

    {{ include('fabric/_delete_form.html.twig') }}
{% endblock %}